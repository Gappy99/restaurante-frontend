// Servicio HTTP para lectura de menús orientados al cliente.
// Responsabilidades:
// - Proveer funciones para obtener menús por restaurante.
// - Mantener solo llamadas GET (sin mutaciones), así no afecta la lógica admin.

export async function getMenusByRestaurant(restaurantId) {
  // Implementación real debe usar `shared/api` clients (fetch/axios) del proyecto.
  // Aquí esqueleto: devolver un array vacío para evitar errores al importar.
  return []
}
